# This file was autogenerated and will overwrite each time you run travis_pypi_setup.py
deploy:
  true:
    repo: ehloonion/onionrouter
    tags: true
  distributions: sdist bdist_wheel
  provider: pypi
env:
  matrix:
    - TOXENV=py
    - TOXENV=flake8
install: pip install -U tox
language: python
python:
  - 3.6
  - 3.7
  - 3.8
  - 3.9-dev
script: tox -e ${TOXENV}
